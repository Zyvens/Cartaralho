const{withErrors,ok,fail,requireMethod,getBody}=require('../../lib/http');const{requireUser}=require('../../lib/auth');const roomStore=require('../../lib/roomStore');const{broadcast}=require('../../lib/pusherServer');const{broadcastGameOver}=require('../../lib/roomEvents');
module.exports=withErrors(async(req,res)=>{if(!requireMethod(req,res,'POST'))return;const user=await requireUser(req,res);if(!user)return;const{code,mode}=getBody(req),playerId=String(user.id);if(!code)return fail(res,400,'code é obrigatório.');const room=await roomStore.loadRoom(code);if(!room)return fail(res,404,'Sala não encontrada.');if(String(room.creatorId)!==playerId)return fail(res,403,'Apenas o anfitrião pode realizar esta ação.');if(mode==='end')await broadcastGameOver(room,'O anfitrião encerrou a partida precocemente.',{validForRewards:false,finishReason:'host_ended'});else await broadcast(room.code,'room_cancelled',{message:'O anfitrião cancelou a partida.'});await roomStore.deleteRoom(room.code);ok(res);});

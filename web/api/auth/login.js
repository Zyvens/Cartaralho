const{sql}=require('../../lib/db');const{withErrors,ok,fail,requireMethod,getBody}=require('../../lib/http');const{normalizeUsername,verifyPassword,createSession}=require('../../lib/auth');
module.exports=withErrors(async(req,res)=>{if(!requireMethod(req,res,'POST'))return;const{username,password}=getBody(req),q=normalizeUsername(username);const rows=await sql`SELECT id,username,display_name,email,avatar_data,bio,password_hash,created_at FROM users WHERE lower(username)=${q} OR lower(email)=lower(${q}) LIMIT 1`,user=rows[0];if(!user||!(await verifyPassword(password||'',user.password_hash)))return fail(res,401,'Usuário/e-mail ou senha inválidos.');await sql`UPDATE users SET last_login_at=now() WHERE id=${user.id}`;const token=await createSession(user.id);delete user.password_hash;ok(res,{token,user});});

const{sql}=require('../../lib/db');const{withErrors,ok,fail,requireMethod,getBody}=require('../../lib/http');const{requireUser}=require('../../lib/auth');
module.exports=withErrors(async(req,res)=>{if(!requireMethod(req,res,'POST'))return;const user=await requireUser(req,res);if(!user)return;const{id,favorite}=getBody(req),cardId=Number(id);if(!cardId)return fail(res,400,'Carta inválida.');const rows=await sql`UPDATE user_cards SET is_favorite=${!!favorite},updated_at=now() WHERE id=${cardId} AND user_id=${user.id} RETURNING id,is_favorite`;if(!rows.length)return fail(res,404,'Carta não encontrada.');ok(res,{card:rows[0]});});

const{sql}=require('../../lib/db');const{withErrors,ok,requireMethod}=require('../../lib/http');const{requireUser,createRecoveryCode,tokenHash}=require('../../lib/auth');module.exports=withErrors(async(req,res)=>{if(!requireMethod(req,res,'POST'))return;const u=await requireUser(req,res);if(!u)return;const code=createRecoveryCode();await sql`UPDATE users SET recovery_hash=${tokenHash(code)} WHERE id=${u.id}`;ok(res,{recoveryCode:code,message:'O código anterior deixou de funcionar. Guarde este novo código.'});});

const{withErrors,ok,requireMethod}=require('../../lib/http');const{requireUser}=require('../../lib/auth');const{syncUnlocks,RARITIES}=require('../../lib/metaGame');const{syncMissions}=require('../../lib/missionService');const{sql}=require('../../lib/db');const prestige=require('../../lib/prestigeService');
const SPECIAL_FRAMES={
 'mission-weekly':{key:'mission-weekly',name:'Missão Cumprida',icon:'🎯',rarity:'superrare',description:'Complete todas as missões semanais legadas de uma mesma semana.',target:1,progress:1,unlocked:true},
 'xp-10000':{key:'xp-10000',name:'Viciado(a) Oficial',icon:'⚡',rarity:'legendary',description:'Acumule 10.000 XP em missões e Legado.',target:1,progress:1,unlocked:true}
};
const RARITY_LABELS={common:'Comum',rare:'Incomum',superrare:'Raro',epic:'Épico',legendary:'Lendário',celestial:'Celestial'};
function rarityInfo(key){if(key==='celestial')return prestige.rarityInfo('celestial');const base=RARITIES[key]||RARITIES.common||{};return{...base,label:RARITY_LABELS[key]||base.label||'Comum'};}
module.exports=withErrors(async(req,res)=>{if(!requireMethod(req,res,'GET'))return;const user=await requireUser(req,res);if(!user)return;const[state,missions,p11]=await Promise.all([syncUnlocks(user.id),syncMissions(user.id),prestige.profileState(user.id)]),fresh=(await sql`SELECT equipped_title_key,equipped_frame_key,xp FROM users WHERE id=${user.id}`)[0]||{},extra=await sql`SELECT unlock_key FROM user_unlocks WHERE user_id=${user.id} AND unlock_type='frame'`;const titles=[...state.titles.map(t=>({...t,rarityInfo:rarityInfo(t.rarity)})),...p11.titles],frames=[...state.frames.map(f=>({...f,rarityInfo:rarityInfo(f.rarity)})),...p11.frames],achievements=(state.achievements||[]).map(a=>({...a,rarityInfo:rarityInfo(a.rarity)}));for(const r of extra){const f=SPECIAL_FRAMES[r.unlock_key];if(f&&!frames.some(x=>x.key===f.key))frames.push({...f,rarityInfo:rarityInfo(f.rarity)});}const rarities={...Object.fromEntries(Object.keys(RARITIES).map(k=>[k,rarityInfo(k)])),celestial:prestige.rarityInfo('celestial')};ok(res,{rarities,metrics:state.metrics,titles,frames,achievements,equipped:{titleKey:fresh.equipped_title_key||null,frameKey:fresh.equipped_frame_key||null},xp:Number(fresh.xp||missions.xp||0),level:Math.floor(Number(fresh.xp||missions.xp||0)/1000)+1,missions:missions.missions,newUnlocks:state.newUnlocks||[],royalties:state.royalties||[],achievementsV2Enabled:process.env.ACHIEVEMENTS_V2_ENABLED!=='false',cosmeticsFeatureEnabled:process.env.COSMETICS_FEATURE_ENABLED!=='false',prestige:{ownerships:p11.ownerships,entitlements:p11.entitlements}});});

const{sql}=require('../../lib/db');const{withErrors,ok,requireMethod}=require('../../lib/http');const{requireUser}=require('../../lib/auth');
module.exports=withErrors(async(req,res)=>{if(!requireMethod(req,res,'GET'))return;const user=await requireUser(req,res);if(!user)return;const rows=await sql`SELECT mp.room_code,mp.nickname,mp.avatar_data,mp.final_score,mp.rounds_won,mp.won_match,mp.finished_at,mh.ranking,s.name AS season_name FROM match_players mp LEFT JOIN match_history mh ON mh.room_code=mp.room_code LEFT JOIN seasons s ON s.id=mp.season_id WHERE mp.user_id=${user.id} ORDER BY mp.finished_at DESC LIMIT 50`;ok(res,{matches:rows});});

const { withErrors, ok, fail, requireMethod, getBody } = require('../../lib/http');
const gameManager = require('../../lib/gameManager');
const roomStore = require('../../lib/roomStore');
const { broadcast } = require('../../lib/pusherServer');
const { applyPresenceSweep } = require('../../lib/roomEvents');
const playerStats = require('../../lib/playerStats');
module.exports=withErrors(async(req,res)=>{if(!requireMethod(req,res,'POST'))return;const{playerId,code,cardIndex}=getBody(req);if(!playerId||!code)return fail(res,400,'playerId e code são obrigatórios.');const room=await roomStore.loadRoom(code);if(!room)return fail(res,404,'Sala não encontrada.');const swept=await applyPresenceSweep(room);if(swept.deleted)return fail(res,404,'Sala não encontrada.');const{allPlayed,playedCard}=gameManager.playCard(room,playerId,cardIndex);const player=room.players.get(playerId);if(player?.userId&&playedCard)await playerStats.recordUse(room.code,player.userId,'whiteCards',playedCard);const nonHost=room.playerOrder.filter(id=>id!==room.currentRound.hostId&&room.players.has(id)&&room.players.get(id).connected);let submissions=null;if(allPlayed){const entries=Array.from(room.currentRound.submissions.entries());for(let i=entries.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[entries[i],entries[j]]=[entries[j],entries[i]];}room.currentRound.submissions=new Map(entries);submissions=entries.map(([,s],index)=>({index,card:s.card}));}await roomStore.saveRoom(room);await broadcast(room.code,'card_played',{submissionCount:room.currentRound.submissions.size,totalExpected:nonHost.length});if(submissions)await broadcast(room.code,'all_cards_played',{submissions,blackCard:room.currentRound.blackCard});ok(res);});

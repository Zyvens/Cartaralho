(()=>{const source=s=>s==='pack_best_world'?'Melhores Cartas do Mundo':s==='pack_random'?'Pack Sem Criatividade':s||'Aquisição';window.MarketInventory={render(body,m){const cards=m.data.packCards||[];body.innerHTML=`${m.wallets()}<section class="market-section"><div class="market-section-title"><div><h3>Cartas adquiridas em packs</h3><p>Autoria original preservada; progressão pessoal começa em zero ao entrar no seu inventário.</p></div></div><div class="market-list">${cards.map(x=>`<div class="market-row market-pack-card"><span>${x.card_type==='white'?'🤍':'🖤'}</span><div><b>${m.esc(x.display_text)}</b><small>Por ${m.esc((x.authors||[]).join(', ')||'autoria legada')} · ${m.date(x.acquired_at)}</small></div><em>${m.esc(source(x.acquisition_source))}</em></div>`).join('')||'<div class="market-empty">Nenhuma carta de pack no inventário.</div>'}</div></section>`;}};})();

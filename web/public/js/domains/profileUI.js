'use strict';
(()=>{
 if(window.CartProfileDomain)return;
 CartDomains.claim('profileUI','domains/profileUI.js',()=>{
  const COLORS={common:'#f4f4f5',rare:'#22c55e',superrare:'#3b82f6',epic:'#a855f7',legendary:'#facc15',celestial:'#bff7ff'};
  const RARITY_ORDER={common:1,rare:2,superrare:3,epic:4,legendary:5,celestial:6};
  const RARITY_LABEL={common:'Comum',rare:'Incomum',superrare:'Raro',epic:'Épico',legendary:'Lendário',celestial:'Celestial'};
  const PROGRESSION_FRAME_NAMES={bronze:'Bronze',silver:'Prata',gold:'Ouro',platinum:'Platina'};
  const PROGRESSION_FRAME_DESCRIPTIONS={bronze:'Tenha 5 cartas Bronze no seu baralho. Depois de desbloqueada, esta moldura pode ser equipada livremente no Perfil.',silver:'Tenha 5 cartas Prata no seu baralho. Depois de desbloqueada, esta moldura pode ser equipada livremente no Perfil.',gold:'Tenha 5 cartas Ouro no seu baralho. Depois de desbloqueada, esta moldura pode ser equipada livremente no Perfil.',platinum:'Tenha 5 cartas Platina no seu baralho. Depois de desbloqueada, esta moldura pode ser equipada livremente no Perfil.'};
  const GENESIS={key:'genese-celestial',name:'Gênese',icon:'✦',rarity:'celestial',description:'A moldura que existia antes do catálogo. Exclusiva de quem fez o Cartaralho existir.',target:1,progress:1,unlocked:true,isEntitlement:true,rarityInfo:{key:'celestial',label:'Celestial',color:'#dffbff',order:6,effect:'iridescent'}};
  const esc=v=>{const d=document.createElement('div');d.textContent=String(v??'');return d.innerHTML;};
  const attr=v=>esc(v).replace(/"/g,'&quot;');
  const frameClasses=node=>[...(node?.classList||[])].filter(c=>c.startsWith('frame-'));
  function normalizeProfileData(pm=window.ProfileModal){if(!pm?.data)return;pm.data.titles=Array.isArray(pm.data.titles)?pm.data.titles:[];pm.data.frames=Array.isArray(pm.data.frames)?pm.data.frames:[];pm.data.equipped=pm.data.equipped||{titleKey:null,frameKey:null};pm.data.titles.forEach(t=>{t.rarityInfo={...(t.rarityInfo||{}),label:RARITY_LABEL[t.rarity]||t.rarityInfo?.label||'Comum'};});pm.data.frames.forEach(f=>{const key=String(f?.key||'');f.rarityInfo={...(f.rarityInfo||{}),label:RARITY_LABEL[f.rarity]||f.rarityInfo?.label||'Comum'};if(PROGRESSION_FRAME_NAMES[key]){f.name=PROGRESSION_FRAME_NAMES[key];f.description=PROGRESSION_FRAME_DESCRIPTIONS[key];f.progressionFrame=true;}});const entitlements=Array.isArray(pm.data.prestige?.entitlements)?pm.data.prestige.entitlements:[],equipped=pm.data.equipped.frameKey||AuthClient?.user?.equipped_frame_key||null,hasGenesis=entitlements.some(e=>e?.entitlement_type==='frame'&&e?.entitlement_key==='genese-celestial')||equipped==='genese-celestial';if(hasGenesis&&!pm.data.frames.some(f=>f.key==='genese-celestial'))pm.data.frames.push({...GENESIS});if(!pm.data.equipped.frameKey&&equipped==='genese-celestial')pm.data.equipped.frameKey='genese-celestial';pm.data.titles.sort((a,b)=>(RARITY_ORDER[a.rarity]||99)-(RARITY_ORDER[b.rarity]||99)||String(a.name||'').localeCompare(String(b.name||''),'pt-BR'));pm.data.frames.sort((a,b)=>(RARITY_ORDER[a.rarity]||99)-(RARITY_ORDER[b.rarity]||99)||String(a.name||'').localeCompare(String(b.name||''),'pt-BR'));}
  function keepBasicPhotoPlain(root=document){const node=root.querySelector?.('.profile-modal-avatar-editor-card #profile-modal-avatar-preview .profile-modal-avatar')||document.querySelector('.profile-modal-avatar-editor-card #profile-modal-avatar-preview .profile-modal-avatar');if(!node)return;frameClasses(node).forEach(c=>node.classList.remove(c));}
  function itemRarity(kind,key){if(!key||typeof ProfileModal==='undefined')return'common';const list=kind==='title'?ProfileModal.data?.titles:ProfileModal.data?.frames;return(list||[]).find(x=>String(x.key)===String(key))?.rarity||'common';}
  function colorAppearanceSelectors(root=document){if(typeof ProfileModal==='undefined')return;normalizeProfileData();[['title',root.querySelector?.('[data-profile-draft-title]')||document.querySelector('[data-profile-draft-title]')],['frame',root.querySelector?.('[data-profile-draft-frame]')||document.querySelector('[data-profile-draft-frame]')]].forEach(([kind,select])=>{if(!select)return;[...select.options].forEach(o=>{if(!o.value){o.style.color='#8b8796';return;}const rarity=itemRarity(kind,o.value);o.dataset.rarity=rarity;o.style.color=COLORS[rarity]||COLORS.common;});const rarity=select.value?itemRarity(kind,select.value):'common';select.dataset.rarity=select.value?rarity:'';select.style.setProperty('color',select.value?(COLORS[rarity]||COLORS.common):'#b1adba','important');});}
  function stabilizeGenesis(root=document){root.querySelectorAll?.('.profile-modal-frame-grid').forEach(grid=>{grid.classList.add('p57-live-frame-grid','p58-live-frame-grid');grid.classList.remove('profile-modal-frame-grid');});root.querySelectorAll?.('.p58-live-frame-grid .avatar-frame.frame-genese-celestial,.p57-live-frame-grid .avatar-frame.frame-genese-celestial').forEach(frame=>{window.GenesisFrameP29?.mount?.(frame);frame.classList.add('p58-genesis-preview');});}
  function captureProfileDraft(pm){if(!pm?.overlay)return;const name=pm.overlay.querySelector('#profile-modal-name'),email=pm.overlay.querySelector('#profile-modal-email'),bio=pm.overlay.querySelector('#profile-modal-bio');if(!name&&!email&&!bio)return;pm.__profileDraft={displayName:name?.value??pm.__profileDraft?.displayName??AuthClient.user?.display_name??'',email:email?.value??pm.__profileDraft?.email??AuthClient.user?.email??'',bio:bio?.value??pm.__profileDraft?.bio??AuthClient.user?.bio??''};}
  function bindProfileDraft(pm){if(!pm?.overlay||pm.activeTab!=='profile')return;const d=pm.__profileDraft||{},name=pm.overlay.querySelector('#profile-modal-name'),email=pm.overlay.querySelector('#profile-modal-email'),bio=pm.overlay.querySelector('#profile-modal-bio');if(name&&d.displayName!==undefined)name.value=d.displayName;if(email&&d.email!==undefined)email.value=d.email;if(bio&&d.bio!==undefined)bio.value=d.bio;[name,email,bio].filter(Boolean).forEach(el=>{if(el.__domainDraftBound)return;el.__domainDraftBound=true;el.addEventListener('input',()=>captureProfileDraft(pm));});}
  function ensureAppearanceDraft(pm){if(!pm?.data)return;if(pm._appearanceSaved)return;pm._appearanceSaved={titleKey:pm.data.equipped?.titleKey||null,frameKey:pm.data.equipped?.frameKey||null};pm.data.equipped={...pm._appearanceSaved};pm._appearanceDirty=false;pm.__appearanceDirty=false;}
  function setAppearanceDraft(pm,kind,key){ensureAppearanceDraft(pm);if(!pm.data?.equipped)return;if(kind==='title')pm.data.equipped.titleKey=key||null;else pm.data.equipped.frameKey=key||null;pm._appearanceDirty=pm.data.equipped.titleKey!==pm._appearanceSaved.titleKey||pm.data.equipped.frameKey!==pm._appearanceSaved.frameKey;pm.__appearanceDirty=pm._appearanceDirty;syncAppearanceDom(pm);}
  function syncAppearanceDom(pm){if(!pm?.overlay||!pm.data)return;normalizeProfileData(pm);const d=pm.data,titleKey=d.equipped?.titleKey||null,frameKey=d.equipped?.frameKey||null,title=d.titles.find(x=>x.key===titleKey)||null,frame=d.frames.find(x=>x.key===frameKey)||null,root=pm.overlay,titleSelect=root.querySelector('[data-profile-draft-title]'),frameSelect=root.querySelector('[data-profile-draft-frame]');if(titleSelect)titleSelect.value=titleKey||'';if(frameSelect)frameSelect.value=frameKey||'';[root.querySelector('.profile-modal-identity .avatar-frame'),root.querySelector('.profile-appearance-live-preview .avatar-frame'),root.querySelector('.profile-modal-frame-hero .avatar-frame')].filter(Boolean).forEach(node=>{frameClasses(node).forEach(c=>node.classList.remove(c));if(frameKey)node.classList.add(`frame-${frameKey}`);});keepBasicPhotoPlain(root);const live=root.querySelector('.profile-appearance-live-preview>div');if(live){let titleNode=live.querySelector('.equipped-title,.profile-appearance-no-title');if(!titleNode){titleNode=document.createElement('span');live.appendChild(titleNode);}if(title){titleNode.className='equipped-title';titleNode.dataset.titleKey=title.key;titleNode.textContent=`${title.icon||'🏷️'} ${title.name}`;}else{titleNode.className='profile-appearance-no-title';titleNode.removeAttribute('data-title-key');titleNode.textContent='Sem título';}let frameNode=live.querySelector('em');if(!frameNode){frameNode=document.createElement('em');live.appendChild(frameNode);}frameNode.textContent=frame?`◉ ${frame.name}`:'◌ Sem moldura';}const handle=root.querySelector('.profile-modal-handle');if(handle){let current=handle.querySelector('.profile-modal-title');if(title){if(!current){current=document.createElement('span');current.className='profile-modal-title';handle.append(' ',current);}current.className=`profile-modal-title rarity-${title.rarity||'common'}`;current.textContent=`${title.icon||'🏷️'} ${title.name}`;}else current?.remove();}root.querySelectorAll('[data-equip-frame]').forEach(btn=>{const active=btn.dataset.equipFrame===frameKey,card=btn.closest('.profile-modal-frame-item'),top=card?.querySelector('.profile-modal-unlock-top');card?.classList.toggle('equipped',active);btn.textContent=active?'Usando':'Equipar';let pill=top?.querySelector('.profile-modal-equipped-pill');if(active&&!pill){pill=document.createElement('span');pill.className='profile-modal-equipped-pill';pill.textContent='EQUIPADO';top?.appendChild(pill);}else if(!active)pill?.remove();});root.querySelectorAll('[data-equip-title]').forEach(btn=>{const active=btn.dataset.equipTitle===titleKey,card=btn.closest('.profile-modal-unlock'),top=card?.querySelector('.profile-modal-unlock-top');card?.classList.toggle('equipped',active);btn.textContent=active?'Usando':'Equipar';let pill=top?.querySelector('.profile-modal-equipped-pill');if(active&&!pill){pill=document.createElement('span');pill.className='profile-modal-equipped-pill';pill.textContent='EQUIPADO';top?.appendChild(pill);}else if(!active)pill?.remove();});const footer=root.querySelector('.profile-global-footer');if(footer){footer.classList.toggle('is-dirty',!!pm._appearanceDirty);const copy=footer.querySelector('small');if(copy)copy.textContent=pm._appearanceDirty?'Há alterações de aparência aguardando o Salvar alterações.':'Perfil, título e moldura são confirmados juntos.';}colorAppearanceSelectors(root);MetaUI?.decorateTitles?.();}
  function profileAppearanceCard(pm){normalizeProfileData(pm);const d=pm.data||{},titles=(d.titles||[]).filter(x=>x.unlocked),frames=(d.frames||[]).filter(x=>x.unlocked),t=d.equipped?.titleKey||'',f=d.equipped?.frameKey||'',title=titles.find(x=>x.key===t),frame=frames.find(x=>x.key===f),u=AuthClient?.user||{};return `<section class="profile-modal-card profile-appearance-selector-card"><div class="profile-modal-section-heading"><div><span>APARÊNCIA DA PARTIDA</span><h3>Título e moldura</h3></div></div><p class="profile-appearance-help">Escolha como seu perfil aparece no Lobby e durante a partida. Molduras de progressão Bronze, Prata, Ouro e Platina continuam disponíveis para escolha mesmo após desbloquear níveis superiores.</p><div class="profile-appearance-live-preview">${pm.avatar(pm.draftAvatar,76,f||null)}<div><small>PRÉVIA PÚBLICA</small><b>${esc(u.display_name||'Jogador')}</b>${title?`<span class="equipped-title" data-title-key="${attr(title.key)}">${esc(title.icon||'🏷️')} ${esc(title.name)}</span>`:'<span class="profile-appearance-no-title">Sem título</span>'}${frame?`<em>◉ ${esc(frame.name)}</em>`:'<em>◌ Sem moldura</em>'}</div></div><div class="profile-appearance-selectors"><label><span>Título</span><select class="input" data-profile-draft-title><option value="">Sem título</option>${titles.map(x=>`<option value="${attr(x.key)}" ${t===x.key?'selected':''}>${esc(x.icon||'🏷️')} ${esc(x.name)} · ${esc(x.rarityInfo?.label||RARITY_LABEL[x.rarity]||x.rarity)}</option>`).join('')}</select></label><label><span>Moldura</span><select class="input" data-profile-draft-frame><option value="">Sem moldura</option>${frames.map(x=>`<option value="${attr(x.key)}" ${f===x.key?'selected':''}>${esc(x.icon||'◉')} ${esc(x.name)} · ${esc(x.rarityInfo?.label||RARITY_LABEL[x.rarity]||x.rarity)}</option>`).join('')}</select></label></div></section>`;}
  function ensureFooter(pm){const shell=pm?.overlay?.querySelector('.profile-modal-shell');if(!shell)return;if(!shell.__domainDraftCapture){shell.__domainDraftCapture=true;shell.addEventListener('click',e=>{if(e.target.closest('[data-profile-tab]'))captureProfileDraft(pm);},true);}let footer=shell.querySelector('.profile-global-footer');if(!footer){footer=document.createElement('footer');footer.className='profile-global-footer';footer.innerHTML='<div class="profile-global-footer-copy"><b>Configuração da conta</b><small>Perfil, título e moldura são confirmados juntos.</small></div><button type="button" class="btn btn-primary profile-global-save">Salvar alterações</button>';shell.appendChild(footer);footer.querySelector('.profile-global-save').onclick=()=>saveAll(pm);}syncAppearanceDom(pm);}
  async function saveAll(pm){captureProfileDraft(pm);const btn=pm?.overlay?.querySelector('.profile-global-save');if(!btn||!AuthClient.user)return;btn.disabled=true;btn.classList.add('is-saving');btn.textContent='Salvando...';try{const d=pm.__profileDraft||{displayName:AuthClient.user.display_name,email:AuthClient.user.email||'',bio:AuthClient.user.bio||''},titleKey=pm.data?.equipped?.titleKey||null,frameKey=pm.data?.equipped?.frameKey||null;await AuthClient.saveProfile({displayName:d.displayName,email:d.email,bio:d.bio,avatarData:pm.draftAvatar,titleKey,frameKey});pm.data.equipped={titleKey:AuthClient.user?.equipped_title_key||null,frameKey:AuthClient.user?.equipped_frame_key||null};pm._appearanceSaved={...pm.data.equipped};pm._appearanceDirty=false;pm.__appearanceDirty=false;if(window.App?.state){App.state.matchTitleKey=pm.data.equipped.titleKey;App.state.matchFrameKey=pm.data.equipped.frameKey;}pm.__profileDraft={displayName:AuthClient.user.display_name,email:AuthClient.user.email||'',bio:AuthClient.user.bio||''};HomeScreen.renderAccount();Toast.success('Alterações salvas.');pm.render();}catch(e){Toast.error(e.message||'Não foi possível salvar.');}finally{const fresh=pm?.overlay?.querySelector('.profile-global-save');if(fresh){fresh.disabled=false;fresh.classList.remove('is-saving');fresh.textContent='Salvar alterações';}}}
  function install(){if(typeof ProfileModal==='undefined'||ProfileModal.__domainOwned)return;const P=ProfileModal;P.__domainOwned=true;const baseOpen=P.open.bind(P),baseClose=P.close.bind(P),baseRender=P.render.bind(P),baseRenderProfile=P.renderProfile.bind(P),baseRenderFrames=P.renderFrames.bind(P),baseRenderTab=P.renderTab.bind(P),fallbackMission=P.missionCard?.bind(P);
   P.open=async function(tab='profile'){this.__profileDraft={displayName:AuthClient.user?.display_name||'',email:AuthClient.user?.email||'',bio:AuthClient.user?.bio||''};this._appearanceSaved=null;this._appearanceDirty=false;this.__appearanceDirty=false;const out=await baseOpen(tab);normalizeProfileData(this);ensureAppearanceDraft(this);return out;};
   P.close=function(){this._appearanceSaved=null;this._appearanceDirty=false;this.__appearanceDirty=false;this.__profileDraft=null;return baseClose();};
   P.render=function(...args){normalizeProfileData(this);ensureAppearanceDraft(this);const out=baseRender(...args);ensureFooter(this);return out;};
   P.renderProfile=function(body){const out=baseRenderProfile(body),repaint=()=>{const p=body.querySelector('#profile-modal-avatar-preview');if(p)p.innerHTML=this.avatar(this.draftAvatar,116,null);keepBasicPhotoPlain(body);};repaint();const file=body.querySelector('#profile-modal-avatar-file');if(file)file.onchange=async e=>{try{const chosen=e.target.files?.[0];if(!chosen)return;this.draftAvatar=await AuthClient.imageToAvatar(chosen);repaint();}catch(err){Toast.error(err.message);}};const remove=body.querySelector('#profile-modal-avatar-remove');if(remove)remove.onclick=()=>{this.draftAvatar=null;repaint();};body.querySelector('.profile-modal-savebar')?.remove();return out;};
   P.renderFrames=function(body,...args){normalizeProfileData(this);const out=baseRenderFrames(body,...args);stabilizeGenesis(body);requestAnimationFrame(()=>stabilizeGenesis(body));return out;};
   P.renderTab=function(...args){captureProfileDraft(this);normalizeProfileData(this);const out=baseRenderTab(...args),body=this.overlay?.querySelector('#profile-modal-body');if(body&&this.activeTab==='profile'){const layout=body.querySelector('.profile-tab-layout-profile');if(layout&&!layout.querySelector('.profile-appearance-selector-card'))layout.insertAdjacentHTML('beforeend',profileAppearanceCard(this));}body?.querySelectorAll('.profile-modal-savebar,.profile-appearance-savebar').forEach(x=>x.remove());bindProfileDraft(this);body?.querySelector('[data-profile-draft-title]')?.addEventListener('change',e=>setAppearanceDraft(this,'title',e.target.value||null));body?.querySelector('[data-profile-draft-frame]')?.addEventListener('change',e=>setAppearanceDraft(this,'frame',e.target.value||null));keepBasicPhotoPlain();stabilizeGenesis();ensureFooter(this);syncAppearanceDom(this);return out;};
   P.equipTitle=function(key){setAppearanceDraft(this,'title',key||null);};P.equipFrame=function(key){setAppearanceDraft(this,'frame',key||null);};P.saveAppearance=async function(){this.overlay?.querySelector('.profile-global-save')?.click();};P._appearanceSavebar=()=>'';P._profileAppearanceCard=function(){return profileAppearanceCard(this);};P._syncAppearanceDom=function(){syncAppearanceDom(this);};P.rarityLegend=()=>'<span class="rarity-common">● Comum</span><span class="rarity-rare">● Incomum</span><span class="rarity-superrare">● Raro</span><span class="rarity-epic">● Épico</span><span class="rarity-legendary">● Lendário</span><span class="rarity-celestial">✦ Celestial</span>';
   if(fallbackMission)P.missionCard=function(m){return window.CartMissionsDomain?.missionRow?.(m)||window.MetaUI?.missionRow?.(m)||fallbackMission(m);};
  }
  install();document.addEventListener('change',e=>{if(e.target.matches('[data-profile-draft-title],[data-profile-draft-frame]'))queueMicrotask(()=>{colorAppearanceSelectors();keepBasicPhotoPlain();});});window.addEventListener('pageshow',()=>{install();normalizeProfileData();stabilizeGenesis();});window.CartProfileDomain={install,keepBasicPhotoPlain,colorAppearanceSelectors,stabilizeGenesis,normalizeProfileData,profileAppearanceCard,saveAll,setAppearanceDraft,syncAppearanceDom,PROGRESSION_FRAME_NAMES};
 });
})();

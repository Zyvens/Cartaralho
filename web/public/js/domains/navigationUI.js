'use strict';
(()=>{
 if(window.CartNavigationDomain)return;
 CartDomains.claim('navigationUI','domains/navigationUI.js',()=>{
  const ORDER=['#marketplace-menu-btn','#friends-menu-btn','[data-panel="cards"]','[data-panel="rank"]','#notifications-menu-btn','[data-panel="history"]','[data-panel="stats"]','#audio-settings-menu-btn','[data-panel="credits"]'];
  function navHost(){let h=document.getElementById('domain-top-nav-host');if(!h){h=document.createElement('div');h.id='domain-top-nav-host';h.className='p48-top-nav-host';document.body.appendChild(h);}return h;}
  function clearBack(){document.querySelectorAll('#domain-top-nav-host>.p48-promoted-back').forEach(b=>b.remove());}
  function promoteBack(){const b=document.querySelector('#app button.back-button');if(!b)return;b.classList.add('p48-promoted-back');navHost().appendChild(b);}
  function playFormOpen(){const el=document.getElementById('play-form');return !!el&&getComputedStyle(el).display!=='none';}
  function syncHomeBack(){const home=document.querySelector('.home-screen'),form=document.getElementById('play-form'),back=document.getElementById('back-play');if(!home||!form||!back)return;if(playFormOpen()){if(back.parentElement!==home)home.appendChild(back);back.classList.add('p42-home-back');back.style.removeProperty('display');}else{if(back.parentElement!==form)form.prepend(back);back.classList.remove('p42-home-back');}}
  function orderMenu(){if(window.HomeMenuP27?.apply)HomeMenuP27.apply();const actions=document.querySelector('#home-main .profile-actions');if(!actions)return;ORDER.forEach((s,i)=>actions.querySelector(s)?.style.setProperty('order',String(i+1),'important'));document.getElementById('friends-menu-btn')?.querySelector('.home-action-arrow')?.setAttribute('aria-hidden','true');}
  function scheduleHome(){for(const ms of[0,40,120,260])setTimeout(()=>{syncHomeBack();orderMenu();window.CartAdminDomain?.ensure?.();},ms);}
  function installStableBackground(){if(HomeScreen.__domainBackground)return;HomeScreen.__domainBackground=true;HomeScreen.__domainBgGeneration=0;HomeScreen.__domainBgTimeouts=new Set();HomeScreen.stopDynamicBackground=function(){this.__domainBgGeneration=(this.__domainBgGeneration||0)+1;if(this.bgInterval){clearInterval(this.bgInterval);this.bgInterval=null;}for(const id of this.__domainBgTimeouts||[])clearTimeout(id);this.__domainBgTimeouts?.clear();};HomeScreen.initDynamicBackground=function(){this.stopDynamicBackground();const generation=this.__domainBgGeneration,container=document.getElementById('dynamic-bg-container');if(!container)return;const later=(fn,ms)=>{const id=setTimeout(()=>{this.__domainBgTimeouts.delete(id);fn();},ms);this.__domainBgTimeouts.add(id);return id;};fetch('/api/sample-cards').then(r=>r.json()).then(data=>{if(generation!==this.__domainBgGeneration||document.getElementById('dynamic-bg-container')!==container)return;const all=[...(data.blackCards||[]).map(c=>({text:c.text,type:'black'})),...(data.whiteCards||[]).map(c=>({text:c.text,type:'white'}))];if(!all.length)return;let count=0;const valid=()=>generation===this.__domainBgGeneration&&document.getElementById('dynamic-bg-container')===container&&!document.hidden;const spawn=()=>{if(!valid()||count>=6)return;count++;const c=all[Math.floor(Math.random()*all.length)],el=document.createElement('div');el.className=`dynamic-floating-card ${c.type}`;el.style.left=`${10+Math.random()*80}%`;el.style.top=`${10+Math.random()*80}%`;el.innerHTML=`<div class="card-watermark">Cartaralho</div><div class="card-text">${HomeScreen.escape(c.text)}</div>`;container.appendChild(el);later(()=>{if(el.isConnected)el.style.opacity='1';},50);later(()=>{if(el.isConnected)el.style.opacity='0';later(()=>{el.remove();count=Math.max(0,count-1);},800);},4500+Math.random()*3000);};for(let i=0;i<3;i++)later(spawn,i*400);this.bgInterval=setInterval(()=>{if(valid())spawn();},2200);}).catch(()=>{});};}
  function install(){if(App.__domainNavigation)return;App.__domainNavigation=true;installStableBackground();const base=App.showScreen.bind(App);App.showScreen=function(name,data={}){document.body.dataset.cartScreen=name||App.state?.currentScreen||'home';clearBack();if(name!=='home')HomeScreen.stopDynamicBackground?.();const out=base(name,data);setTimeout(()=>{promoteBack();if(name==='home'){orderMenu();scheduleHome();}window.CartAdminDomain?.ensure?.();},325);return out;};document.addEventListener('click',e=>{if(e.target.closest('#btn-play,#back-play'))scheduleHome();});document.addEventListener('visibilitychange',()=>{if(document.hidden)HomeScreen.stopDynamicBackground?.();else if(App.state?.currentScreen==='home'&&document.querySelector('.home-screen'))HomeScreen.initDynamicBackground?.();});window.addEventListener('pagehide',()=>HomeScreen.stopDynamicBackground?.());}
  install();window.addEventListener('pageshow',scheduleHome);window.CartNavigationDomain={install,orderMenu,syncHomeBack,promoteBack,scheduleHome};
 });
})();

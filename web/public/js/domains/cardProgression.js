'use strict';
(()=>{
 if(window.CartCardProgression)return;
 CartDomains.claim('cardProgression','domains/cardProgression.js',()=>{
  const LABEL={standard:'Padrão',copper:'Bronze',bronze:'Bronze',silver:'Prata',gold:'Ouro',platinum:'Platina'};
  const TIER_RANK={standard:0,copper:1,bronze:1,silver:2,gold:3,platinum:4};
  const RARITY=[{key:'common',label:'Comum'},{key:'uncommon',label:'Incomum'},{key:'rare',label:'Rara'},{key:'epic',label:'Épica'},{key:'legendary',label:'Lendária'}];
  const fmt=v=>Number(v||0).toLocaleString('pt-BR');
  const esc=v=>{const d=document.createElement('div');d.textContent=String(v??'');return d.innerHTML;};
  const label=v=>LABEL[String(v||'standard').toLowerCase()]||String(v||'Padrão');
  const tierRank=v=>TIER_RANK[String(v||'standard').toLowerCase()]??0;
  function track(kind,c){const background=kind==='material',progress=background?c?.materialProgress:c?.borderProgress,current=background?c?.materialTier:c?.borderTier;const explicit=background?c?.personalRoundWins:c?.lootCollectors,now=Number(explicit??progress?.current??0),remaining=Math.max(0,Number(progress?.remaining||0)),next=progress?.nextTier||null,goal=next?Number(progress?.target??(now+remaining)):now,pct=next?Math.max(3,Math.min(100,goal>0?(now/goal)*100:0)):100;const title=background?'FUNDO':'BORDA',metric=background?'rodadas vencidas com esta carta':'coletas por Espólio por outros jogadores';const rule=background?'O fundo sobe quando você vence uma rodada utilizando esta carta. Ele indica quão boa esta carta é nas partidas.':'A borda sobe quando outros jogadores coletam esta carta em Espólios. Ela indica a popularidade da carta.';return `<article class="p56-progress-track"><div class="p56-progress-track-head"><div><span>${title}</span><b>${label(current)}</b></div><em>${next?`+${fmt(remaining)} → ${label(next)}`:'NÍVEL MÁXIMO'}</em></div><div class="p56-progress-metric"><strong>${fmt(now)}</strong><span>${metric}</span>${next?`<small>${fmt(goal)} para ${label(next)}</small>`:'<small>Progressão concluída</small>'}</div><div class="p56-progress-bar"><i style="width:${pct}%"></i></div><p>${rule}</p></article>`;}
  function winLossCoefficient(card){const wins=Math.max(0,Number(card?.globalWins??card?.origin?.globalWins??0)),losses=Math.max(0,Number(card?.globalLosses??card?.origin?.globalLosses??0));if(!Number.isFinite(wins)||!Number.isFinite(losses))return null;if(losses===0)return wins>0?1:null;const v=wins/losses;return Number.isFinite(v)?v:null;}
  function rarityState(card){const rank=Math.min(tierRank(card?.materialTier),tierRank(card?.borderTier)),base=RARITY[Math.max(0,Math.min(4,rank))]||RARITY[0],coefficient=winLossCoefficient(card);if(rank>=4&&coefficient!==null&&coefficient>=.8)return{key:'super-trunfo',label:'Super Trunfo',rank:5,coefficient};return{...base,rank,coefficient};}
  function formatDate(v){if(!v)return'—';const d=new Date(v);return Number.isNaN(d.getTime())?'—':d.toLocaleDateString('pt-BR',{day:'2-digit',month:'2-digit',year:'numeric'});}
  function creatorData(card){if(card?.is_native)return{name:'Cartaralho',username:null};const o=card?.origin||{};return{name:String(o.creatorName||o.creatorUsername||'Não identificado'),username:o.creatorUsername?`@${String(o.creatorUsername).replace(/^@/,'')}`:null};}
  const cell=(l,v,s=null)=>`<div><small>${esc(l)}</small><b>${esc(v)}</b>${s?`<em>${esc(s)}</em>`:''}</div>`;
  function decorateDetail(overlay,card){if(!overlay||!card)return;const rarity=rarityState(card),shell=overlay.querySelector('.p56-card-detail-shell'),tags=overlay.querySelector('.p56-card-tags'),grid=overlay.querySelector('.p56-origin-grid'),o=card.origin||{},creator=creatorData(card);if(shell){[...shell.classList].filter(x=>x.startsWith('p68-rarity-')).forEach(x=>shell.classList.remove(x));shell.classList.add('p68-rarity-themed',`p68-rarity-${rarity.key}`);shell.dataset.cardRarity=rarity.key;}if(tags){tags.querySelectorAll('.p68-rarity-pill').forEach(x=>x.remove());[...tags.children].forEach(x=>{if(/\b(?:Branca|Preta)\b/i.test(x.textContent||''))x.remove();});const pill=document.createElement('span');pill.className=`p68-rarity-pill p68-rarity-pill-${rarity.key}`;pill.textContent=rarity.label;if(rarity.key==='super-trunfo')pill.title='Super Trunfo: Lendária com coeficiente global de vitórias/derrotas igual ou superior a 80%.';tags.prepend(pill);}if(grid){const seen=Math.max(0,Number(card.externalPresenceMatches??o.seenByCurrentUser??0)),holders=Math.max(0,Number(card.worldHolders??o.holders??0)),plays=Math.max(0,Number(card.globalPlays??o.globalPlays??o.tablesVisited??0)),wins=Math.max(0,Number(card.globalWins??o.globalWins??0)),losses=Math.max(0,Number(card.globalLosses??o.globalLosses??Math.max(0,plays-wins)));grid.classList.add('p68-origin-grid');grid.innerHTML=[cell('CRIADO POR',creator.name,creator.username),cell('DATA',formatDate(o.createdAt||card.created_at)),cell('PRIMEIRA MESA',o.firstRoomCode||'—'),cell('AVISTADA EM JOGOS',fmt(seen)),cell('PESSOAS QUE POSSUEM',fmt(holders)),cell('MESAS VISITADAS',fmt(plays)),cell('MESAS VENCEDORAS',fmt(wins)),cell('MESAS PERDEDORAS',fmt(losses))].join('');}return rarity;}
  window.CartCardProgression={label,fmt,track,tierRank,rarityState,winLossCoefficient,decorateDetail,creatorData,formatDate};
 });
})();

'use strict';
(()=>{
 if(window.CartRankDomain)return;
 CartDomains.claim('rankUI','domains/rankUI.js',()=>{
  const state={rank:null};
  const esc=v=>{const d=document.createElement('div');d.textContent=String(v??'');return d.innerHTML;};
  const titleName=key=>window.IdentityUI?.titleName?.(key)||(window.MetaTitleNames||{})[key]||String(key||'').replace(/-/g,' ').replace(/\b\w/g,c=>c.toUpperCase());
  function copyNode(button){const node=[...button.children].find(el=>el.tagName==='SPAN'&&el.querySelector('b'));if(node)node.classList.add('rank-copy');return node||null;}
  function ensureAvatar(button,row){let avatar=[...button.children].find(el=>el.classList?.contains('user-avatar'))||button.querySelector('.rank-avatar-frame .user-avatar,.rank-avatar-frame img,.rank-avatar-frame .identity-avatar-fallback');if(!avatar){const temp=document.createElement('span');temp.innerHTML=HomeScreen.avatar(row.avatar_data,38);avatar=temp.firstElementChild;const copy=copyNode(button);if(avatar)button.insertBefore(avatar,copy||null);}if(!avatar)return;const frame=row.equipped_frame_key||null;if(!frame)return;const existing=avatar.closest('.avatar-frame');if(existing){existing.classList.add('rank-avatar-frame');return;}if(window.IdentityUI?.wrapExisting){IdentityUI.wrapExisting(avatar,frame);avatar.closest('.avatar-frame')?.classList.add('rank-avatar-frame');return;}const wrapper=document.createElement('span');wrapper.className=`avatar-frame public-avatar-frame rank-avatar-frame frame-${frame}`;avatar.parentNode?.insertBefore(wrapper,avatar);wrapper.appendChild(avatar);}
  function ensureTitle(button,row){const key=row.equipped_title_key||null,copy=copyNode(button);if(!key||!copy)return;let title=copy.querySelector('.rank-equipped-title');if(!title){title=document.createElement('span');title.className='rank-equipped-title equipped-title public-equipped-title';const stats=copy.querySelector('small');copy.insertBefore(title,stats||null);}title.dataset.titleKey=key;title.textContent=titleName(key);}
  function decorate(panel,tab,data=state.rank){if(!panel||tab==='shame'||!data)return;const rows=tab==='fame'?(data.hallOfFame||[]):(data.rank||[]),byUser=new Map(rows.map(r=>[String(r.user_id),r]));panel.querySelectorAll('.rank-player[data-user-id]').forEach(button=>{const row=byUser.get(String(button.dataset.userId));if(row){ensureAvatar(button,row);ensureTitle(button,row);}});MetaUI?.decorateTitles?.();}
  const rankTabs=active=>`<div class="meta-tabs"><button class="meta-tab ${active==='rank'?'active':''}" data-rank-tab="rank">Ranking</button><button class="meta-tab ${active==='shame'?'active':''}" data-rank-tab="shame">Hall da Vergonha</button><button class="meta-tab ${active==='fame'?'active':''}" data-rank-tab="fame">Hall da Fama</button></div>`;
  function bindRankTabs(panel,season){panel.querySelectorAll('[data-rank-tab]').forEach(b=>b.onclick=()=>render(panel,season,b.dataset.rankTab));}
  function bindPlayers(panel){panel.querySelectorAll('.rank-player[data-user-id]').forEach(b=>b.onclick=()=>HomeScreen.renderPublicProfile(panel,b.dataset.userId));}
  async function render(panel,season='current',tab='rank'){
   if(tab==='shame'){const d=await MetaClient.hall();panel.innerHTML=`<div class="home-form profile-panel"><button class="panel-close">✕</button><h3>Rank</h3>${rankTabs('shame')}<h4>Hall da Vergonha</h4>${(d.categories||[]).map(c=>`<div class="hall-category"><h4>${esc(c.icon)} ${esc(c.name)}</h4><small>${esc(c.description)}</small><div class="hall-list">${(c.items||[]).map((x,i)=>`<div class="hall-row"><span>#${i+1}</span><strong>${esc(x.text||'—')}</strong><span>${x.value??x.rooms??0}</span></div>`).join('')||'<p>Sem dados ainda.</p>'}</div></div>`).join('')}</div>`;bindRankTabs(panel,season);return d;}
   const d=await AuthClient.rank(season);state.rank=d;
   if(tab==='fame'){const grouped={};for(const h of d.hallOfFame||[])(grouped[h.season_name]||(grouped[h.season_name]=[])).push(h);panel.innerHTML=`<div class="home-form profile-panel"><button class="panel-close">✕</button><h3>Rank</h3>${rankTabs('fame')}<h4>Hall da Fama</h4><p class="text-muted">Temporadas duram 1 mês. Ao fechar, os três primeiros ficam eternizados aqui e recebem medalha/moldura permanente.</p>${Object.entries(grouped).map(([name,rows])=>`<div class="meta-section"><h4>${esc(name)}</h4>${rows.map(r=>`<button class="rank-player" data-user-id="${r.user_id}"><span style="font-size:1.5rem">${r.position===1?'🥇':r.position===2?'🥈':'🥉'}</span><span><b>${esc(r.display_name)}</b><small>${r.points} pts · ${r.wins} vitórias · ${r.matches} partidas</small></span></button>`).join('')}</div>`).join('')||'<p>Nenhuma temporada encerrada ainda.</p>'}</div>`;bindRankTabs(panel,season);bindPlayers(panel);decorate(panel,'fame',d);return d;}
   panel.innerHTML=`<div class="home-form profile-panel"><button class="panel-close">✕</button><h3>Rank</h3>${rankTabs('rank')}<select id="season-select" class="input"><option value="current">Temporada atual</option><option value="all" ${season==='all'?'selected':''}>Todos os tempos</option>${(d.seasons||[]).map(s=>`<option value="${s.id}" ${String(season)===String(s.id)?'selected':''}>${esc(s.name)}${s.is_active?' · atual':''}</option>`).join('')}</select><div class="rank-list">${(d.rank||[]).map((r,i)=>`<button class="rank-player" data-user-id="${r.user_id}">${HomeScreen.avatar(r.avatar_data,38)}<span><b>#${i+1} ${esc(r.display_name)}</b><small>${r.points} pts · ${r.wins} vitórias · ${r.matches} partidas</small></span></button>`).join('')||'<p>Sem partidas registradas.</p>'}</div><h4>Últimas partidas</h4><div class="recent-matches">${(d.recentMatches||[]).map(m=>`<div><b>${esc(m.room_code)}</b><span>${esc(m.winner_nickname||'—')} venceu · ${new Date(m.finished_at).toLocaleDateString('pt-BR')}</span></div>`).join('')||'<p>Sem partidas.</p>'}</div></div>`;
   panel.querySelector('#season-select').onchange=e=>render(panel,e.target.value,'rank');bindRankTabs(panel,season);bindPlayers(panel);decorate(panel,'rank',d);return d;
  }
  HomeScreen.renderRank=(panel,season='current')=>render(panel,season,'rank');if(window.MetaUI)MetaUI.renderRank=render;
  window.CartRankDomain={state,decorate,render,rankTabs,bindRankTabs};
 });
})();

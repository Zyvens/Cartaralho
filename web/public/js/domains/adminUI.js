'use strict';
(()=>{
 if(window.CartAdminDomain)return;
 CartDomains.claim('adminUI','domains/adminUI.js',()=>{
  const GLOBAL_CHANNEL='cartaralho-global',state={channel:null,seen:new Set()};
  const opId=()=>globalThis.crypto?.randomUUID?crypto.randomUUID():`admin_${Date.now()}_${Math.random().toString(36).slice(2)}`;
  const isAdmin=()=>Number(AuthClient?.user?.id)===1;
  const mainHomeOpen=()=>App?.state?.currentScreen==='home'&&!!document.querySelector('.home-screen')&&(!document.getElementById('play-form')||getComputedStyle(document.getElementById('play-form')).display==='none');
  function remove(){document.getElementById('creator-admin-fab')?.remove();document.getElementById('creator-admin-overlay')?.remove();document.body.classList.remove('creator-admin-active');}
  function showMegaphone(message,{amount=null}={}){const container=document.getElementById('toast-container');if(!container)return;const toast=document.createElement('div');toast.className='toast info megaphone';const icon=document.createElement('span');icon.className='toast-icon';icon.textContent='📣';const copy=document.createElement('span');copy.className='toast-message';copy.textContent=String(message||'Aviso do Cartaralho');if(Number(amount)>0){const reward=document.createElement('span');reward.className='toast-megaphone-reward';reward.textContent=`+${Number(amount).toLocaleString('pt-BR')} Moedas Sujas`;copy.appendChild(reward);}toast.append(icon,copy);container.appendChild(toast);const timer=setTimeout(()=>Toast._dismiss(toast),10000);toast.addEventListener('click',()=>{clearTimeout(timer);Toast._dismiss(toast);});}
  function handleMegaphone(data={}){const id=data._eventId;if(id&&state.seen.has(id))return;if(id){state.seen.add(id);if(state.seen.size>128)state.seen.delete(state.seen.values().next().value);}const targets=Array.isArray(data.targetUserIds)?data.targetUserIds.map(Number):null;if(targets?.length&&!targets.includes(Number(AuthClient?.user?.id)))return;showMegaphone(data.message,{amount:data.kind==='reward'?data.amount:null});}
  async function subscribe(){try{await SocketClient._waitReady();if(!SocketClient.pusher||state.channel)return;state.channel=SocketClient.pusher.subscribe(GLOBAL_CHANNEL);state.channel.bind('admin_megaphone',handleMegaphone);}catch(e){console.warn('Megafone indisponível',e);}}
  async function serverVersion(){try{const d=await AuthClient.request('/api/version');return String(d?.currentVersion||'');}catch(_){return'';}}
  async function checkVersion(){try{const current=await serverVersion();if(!current)return;const key=`cartaralho_update_notice_${current}`;if(localStorage.getItem(key)==='1'){sessionStorage.setItem(key,'1');return;}if(!sessionStorage.getItem(key)){sessionStorage.setItem(key,'1');localStorage.setItem(key,'1');showMegaphone(`Nova atualização ${current} disponível. Reinicie o jogo para adicionar as atualizações.`);}}catch(_){ }}
  async function post(payload){return AuthClient.request('/api/admin/creator-tools',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});}
  async function runButton(btn,work){if(!btn||btn.disabled)return;const old=btn.innerHTML;btn.disabled=true;btn.textContent='Enviando...';try{await work();}catch(e){Toast.error(e.message);}finally{btn.disabled=false;btn.innerHTML=old;}}
  function targetField(id,label='Usuário / nickname da conta'){return `<label class="creator-admin-field"><span>${label}</span><input id="${id}" class="input" maxlength="80" placeholder="@username ou nome exato"></label>`;}
  function open(){if(!isAdmin())return;document.getElementById('creator-admin-overlay')?.remove();const o=document.createElement('div');o.id='creator-admin-overlay';o.className='creator-admin-overlay';o.innerHTML=`<section class="creator-admin-shell" role="dialog" aria-modal="true"><header class="creator-admin-head"><div><small>ADMIN • VitorIvens</small><h2>🛠 Ferramentas do Criador</h2><p>Comandos administrativos protegidos pela sua sessão.</p></div><button class="creator-admin-close">✕</button></header><div class="creator-admin-body"><section class="creator-admin-card"><h3>📣 Megafone</h3><div class="creator-admin-grid"><label class="creator-admin-field"><span>Destino</span><select id="admin-mega-scope" class="input"><option value="global">Todos os jogadores</option><option value="individual">Jogador específico</option></select></label><div id="admin-mega-target-wrap" style="display:none">${targetField('admin-mega-target')}</div><label class="creator-admin-field full"><span>Mensagem</span><textarea id="admin-mega-message" class="input" maxlength="240"></textarea></label></div><div class="creator-admin-actions"><button id="admin-mega-send" class="btn btn-primary">📣 Megafonar</button><button id="admin-update-send" class="btn btn-secondary">🆕 Avisar nova atualização</button></div></section><section class="creator-admin-card"><h3>🎁 Prêmios</h3><div class="creator-admin-subcard"><h4>Individual</h4><div class="creator-admin-grid">${targetField('admin-reward-target')}<label class="creator-admin-field"><span>Quantidade de moedas</span><input id="admin-reward-amount" class="input" type="number" min="1" max="10000000"></label><label class="creator-admin-field full"><span>Mensagem</span><textarea id="admin-reward-message" class="input" maxlength="240"></textarea></label></div><button id="admin-reward-send" class="btn btn-primary">🎁 Premiar jogador</button></div><div class="creator-admin-subcard"><h4>Coletivo</h4><div class="creator-admin-grid"><label class="creator-admin-field"><span>Quantidade para cada conta</span><input id="admin-reward-all-amount" class="input" type="number" min="1" max="10000000"></label><label class="creator-admin-field full"><span>Mensagem</span><textarea id="admin-reward-all-message" class="input" maxlength="240"></textarea></label></div><button id="admin-reward-all-send" class="btn btn-primary">🎁 Premiar todos</button></div></section><p class="creator-admin-security">O servidor valida novamente a identidade antes de executar qualquer comando e os créditos são protegidos contra duplicação.</p></div></section>`;document.body.appendChild(o);const close=()=>o.remove();o.querySelector('.creator-admin-close').onclick=close;o.addEventListener('mousedown',e=>{if(e.target===o)close();});const scope=o.querySelector('#admin-mega-scope'),targetWrap=o.querySelector('#admin-mega-target-wrap');scope.onchange=()=>targetWrap.style.display=scope.value==='individual'?'block':'none';o.querySelector('#admin-mega-send').onclick=()=>sendMegaphone(scope.value);o.querySelector('#admin-update-send').onclick=()=>sendUpdate();o.querySelector('#admin-reward-send').onclick=()=>sendReward('individual');o.querySelector('#admin-reward-all-send').onclick=()=>sendReward('global');}
  async function sendMegaphone(scope){const btn=document.getElementById('admin-mega-send'),message=document.getElementById('admin-mega-message')?.value.trim(),target=document.getElementById('admin-mega-target')?.value.trim();if(!message)return Toast.warning('Escreva a mensagem do megafone.');if(scope==='individual'&&!target)return Toast.warning('Informe o usuário.');return runButton(btn,async()=>{const d=await post({action:'megaphone',scope,target,message});Toast.success(d.scope==='individual'?`Aviso enviado para ${d.target?.displayName||d.target?.username}.`:'Aviso enviado para todos.');});}
  async function sendUpdate(){const btn=document.getElementById('admin-update-send');return runButton(btn,async()=>{const current=await serverVersion();await post({action:'megaphone',scope:'global',message:`Nova atualização ${current} disponível. Reinicie o jogo para adicionar as atualizações.`});Toast.success(`Aviso da ${current} enviado.`);});}
  async function sendReward(scope){const all=scope==='global',btn=document.getElementById(all?'admin-reward-all-send':'admin-reward-send'),amount=Number(document.getElementById(all?'admin-reward-all-amount':'admin-reward-amount')?.value),message=document.getElementById(all?'admin-reward-all-message':'admin-reward-message')?.value.trim(),target=all?null:document.getElementById('admin-reward-target')?.value.trim();if(!Number.isInteger(amount)||amount<1)return Toast.warning('Informe uma quantidade válida.');if(!message)return Toast.warning('Escreva a mensagem.');if(!all&&!target)return Toast.warning('Informe o usuário premiado.');return runButton(btn,async()=>{const d=await post({action:'reward',scope,target,amount,message,operationId:opId()});if(!all&&d?.balance!==undefined)window.CartMarketplaceDomain?.applyBalance?.(Number(d.balance),{source:'admin_reward_response'});else if(all)setTimeout(()=>window.CartMarketplaceDomain?.refreshBalance?.('admin_reward_global_response'),20);Toast.success(all?`Prêmio enviado para ${d.credited} conta(s).`:`${amount.toLocaleString('pt-BR')} moedas enviadas.`);});}
  function ensure(){if(!isAdmin()||!mainHomeOpen()){remove();return null;}document.body.classList.add('creator-admin-active');let b=document.getElementById('creator-admin-fab');if(!b){b=document.createElement('button');b.id='creator-admin-fab';b.className='creator-admin-fab';b.textContent='🛠 Admin';b.onclick=open;document.body.appendChild(b);}return b;}
  subscribe();setTimeout(checkVersion,900);setInterval(checkVersion,60000);document.addEventListener('visibilitychange',()=>{if(!document.hidden){subscribe();checkVersion();ensure();}});window.CartAdminDomain={ensure,remove,open,showMegaphone,checkVersion,serverVersion,subscribe};
 });
})();

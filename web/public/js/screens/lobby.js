const LobbyScreen={
 render(container,data={}){const code=App.state.roomCode||data.code||'------',mine=App.state.players.find(p=>p.nickname===App.state.nickname),ready=!!mine?.cardsReady,creation=App.state.config?.cardCreationEnabled!==false;container.innerHTML=`<div class="lobby-screen"><div class="room-code-display"><h3 style="color:var(--text-muted);font-size:.85rem;text-transform:uppercase;letter-spacing:.1em">Código da Mesa</h3><div class="room-code-value" id="room-code-copy" title="Clique para copiar">${code}<span class="copy-icon">📋</span></div><p class="room-code-hint">Compartilhe o código com seus amigos.</p></div><div class="lobby-content"><div class="lobby-section"><div class="lobby-section-header"><h3>👥 Jogadores</h3><span class="player-count" id="player-count">0/0</span></div><div id="lobby-player-list"></div></div><div id="lobby-status" class="lobby-status">Aguardando jogadores...</div><div class="lobby-actions">${creation?(ready?'<button class="btn btn-success btn-lg btn-block" disabled>✅ Cartas de Jogador concluídas</button>':'<button id="register-cards-btn" class="btn btn-secondary btn-lg btn-block">⭐ Escolher / Criar Cartas de Jogador</button>'):'<button class="btn btn-success btn-lg btn-block" disabled>✅ Criação de cartas desativada</button>'}${App.state.isCreator?'<button id="start-game-btn" class="btn btn-primary btn-lg btn-block" disabled style="margin-top:8px">🚀 Iniciar Partida</button><button id="cancel-room-btn" class="btn btn-danger btn-lg btn-block" style="margin-top:8px">❌ Cancelar Partida</button>':''}</div></div></div>`;this.init();if(App.state.players?.length)this.update(App.state.players);if(!creation&&!ready)setTimeout(()=>SocketClient.submitCards(App.state.roomCode,[],[],{}),150);},
 update(players){const list=document.getElementById('lobby-player-list');if(!list)return;PlayerList.render(list,players,{showStatus:true,showScore:false,canEdit:false});document.getElementById('player-count').textContent=`${players.length}/${App.state.maxPlayers||'?'}`;const allReady=players.length>=3&&players.every(p=>p.cardsReady),status=document.getElementById('lobby-status'),start=document.getElementById('start-game-btn');if(allReady){status.className='lobby-status all-ready';status.textContent='✅ Todos prontos! A partida pode começar!';}else{const n=players.filter(p=>!p.cardsReady).length;status.className='lobby-status';status.textContent=App.state.config?.cardCreationEnabled===false?`Aguardando ${n} jogador${n===1?'':'es'} ficar pronto...`:`Aguardando ${n} jogador${n===1?'':'es'} concluir Cartas de Jogador...`;}if(start)start.disabled=!allReady;},
 init(){document.getElementById('room-code-copy')?.addEventListener('click',()=>navigator.clipboard.writeText(App.state.roomCode).then(()=>Toast.success('Código copiado!')).catch(()=>{}));document.getElementById('register-cards-btn')?.addEventListener('click',()=>App.showScreen('cardCreation'));const start=document.getElementById('start-game-btn');if(start)start.onclick=()=>{const p=App.state.players.length,max=App.state.maxPlayers;const go=()=>{start.disabled=true;start.innerHTML='<div class="spinner spinner-sm" style="border-top-color:#fff"></div> Iniciando...';if(p<max)SocketClient.updateRoomConfig(App.state.roomCode,{maxPlayers:p});setTimeout(()=>SocketClient.startGame(App.state.roomCode),p<max?350:0);};Modal.show({title:'Iniciar Partida?',message:p<max?`A mesa aceita ${max}, mas há ${p} jogadores. Começar agora?`:'Todos estão prontos. Iniciar agora?',confirmText:'Iniciar! 🎮',cancelText:'Aguardar',onConfirm:go});};document.getElementById('cancel-room-btn')?.addEventListener('click',async()=>{if(await Modal.confirm('Atenção','Cancelar a partida para todos?'))SocketClient.emit('cancel_room',{code:App.state.roomCode});});}
};
